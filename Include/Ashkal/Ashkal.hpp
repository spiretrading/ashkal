#ifndef ASHKAL_HPP
#define ASHKAL_HPP

namespace Ashkal {
  struct AmbientLight;
  class Camera;
  struct Color;
  struct DirectionalLight;
  class Matrix;
  struct Mesh;
  struct MeshNode;
  class Model;
  struct Point;
  struct Ray;
  class Scene;
  struct Vector;
  struct Vertex;
}

#endif
