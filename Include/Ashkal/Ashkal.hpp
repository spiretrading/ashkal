#ifndef ASHKAL_HPP
#define ASHKAL_HPP

namespace Ashkal {
  struct AmbientLight;
  class Camera;
  struct Color;
  class ColorSampler;
  struct DirectionalLight;
  class Fragment;
  class Material;
  class Matrix;
  struct Mesh;
  struct MeshNode;
  class Model;
  struct Point;
  struct Ray;
  class Scene;
  struct ShadingSample;
  struct ShadingTerm;
  class SolidColorSampler;
  struct TextureCoordinate;
  struct Vector;
  struct Vertex;
  struct VertexTriangle;
}

#endif
