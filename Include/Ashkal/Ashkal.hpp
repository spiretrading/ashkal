#ifndef ASHKAL_HPP
#define ASHKAL_HPP

namespace Ashkal {
  struct AmbientLight;
  class Camera;
  struct Color;
  struct DirectionalLight;
  class Matrix;
  class Model;
  struct Point;
  struct Ray;
  class Sphere;
  struct Vector;
  struct Voxel;
}

#endif
